# Styling

- Uses vanilla-extract.
- Each `.tsx` has a companion `.css.ts`.
- Export camelCase names without component prefixes (e.g., `container`, not `mapGameContainer`).
- Import as `import * as styles from './Component.css.ts'`, preceded by an empty line.
